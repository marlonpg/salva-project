version: '3.8'

services:
  pdf-generator:
    build: .
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    env_file: .env
    container_name: salva-pdf-generator
